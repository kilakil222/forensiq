{
  "title": "Explicit Credential Use — Event 4648 Network Logon",
  "id": "e2f3a4b5-c6d7-8901-efab-234567890123",
  "status": "stable",
  "description": "Detects use of explicit credentials (Event 4648) which indicates an attacker is impersonating accounts or using harvested credentials for lateral movement. Filters machine accounts and common service noise.",
  "level": "medium",
  "tags": ["attack.lateral_movement", "attack.credential_access", "attack.t1550.002", "attack.t1078"],
  "logsource": {"category": "auth", "product": "windows"},
  "detection": {
    "selection": {
      "EventID": 4648
    },
    "filter_machine": {
      "User|endswith": "$"
    },
    "filter_localhost": {
      "IpAddress": ["127.0.0.1", "-", "::1"]
    },
    "condition": "selection and not filter_machine and not filter_localhost"
  },
  "falsepositives": ["Administrators using runas", "Scheduled tasks running as different accounts", "IIS application pools"]
}
